<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.mapper.AdminUserMapper">

    <resultMap id="AdminUserResultMap" type="com.example.demo.model.AdminUser">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="email" column="email" />
        <result property="password" column="password" />
        <result property="role" column="role" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <!-- username で検索 -->
    <select id="findByUsername" parameterType="string" resultMap="AdminUserResultMap">
        SELECT * FROM admin_users WHERE username = #{username}
    </select>

    <!-- email で検索 -->
    <select id="findByEmail" parameterType="string" resultMap="AdminUserResultMap">
        SELECT * FROM admin_users WHERE email = #{email}
    </select>

</mapper>